apiVersion: v1
kind: Service

metadata:
  name: brain-tasks-service
  namespace: brain-tasks

spec:
  type: LoadBalancer

  selector:
    app: brain-tasks

  ports:
  - protocol: TCP
    port: 80
    targetPort: 3000